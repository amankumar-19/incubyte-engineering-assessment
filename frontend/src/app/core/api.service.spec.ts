import {TestBed} from '@angular/core/testing'; import {provideHttpClient} from '@angular/common/http'; import {provideHttpClientTesting,HttpTestingController} from '@angular/common/http/testing'; import {ApiService} from './api.service';
describe('ApiService',()=>{let s:ApiService,h:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});s=TestBed.inject(ApiService);h=TestBed.inject(HttpTestingController)});afterEach(()=>h.verify());
it('uses server-side pagination',()=>{s.employees({page:2,size:20}).subscribe();let r=h.expectOne(x=>x.url.endsWith('/api/employees')&&x.params.get('page')==='2');expect(r.request.method).toBe('GET');r.flush({content:[],totalElements:0,totalPages:0,size:20,number:2,first:false,last:true})})});
