spring:
  datasource:
    url: jdbc:h2:file:./data/salarydb;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          batch_size: 100
        order_inserts: true
  h2:
    console:
      enabled: true
server:
  port: 8080
